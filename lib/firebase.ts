// Firebase is no longer used. All data is in MongoDB.
// This file exists only to prevent import errors during transition.

export const auth = null;
export const db = null;
export const storage = null;
export default null;
